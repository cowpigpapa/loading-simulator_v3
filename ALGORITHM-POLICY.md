# LoadWise 알고리즘 정책과 한계

- 문서 버전: 1.2
- 기준 서비스: LoadWise v3 공개 베타
- 갱신일: 2026-08-11

이 문서는 LoadWise가 적재안을 만드는 기준, 결과를 해석하는 방법과 현재 계산하지 못하는 영역을 공개한다. 새로운 알고리즘을 추가할 때도 이 문서의 안전 원칙과 변경 절차를 따른다.

## 1. 결과의 성격

LoadWise의 결과는 컨테이너 적재 계획을 빠르게 검토하기 위한 휴리스틱 제안이다. 전역 최적해, 구조 안전 인증 또는 현장 상차 지시를 의미하지 않는다. 실제 적재 전에는 화물 사양, 컨테이너 상태, 운송 조건과 고정재 제조사 지침을 현장 책임자가 확인해야 한다.

## 2. 현재 계산 흐름

1. 직접 입력과 Excel/CSV 데이터를 하나의 제품 모델로 정규화한다.
2. 제품별 허용 회전 방향을 생성한다. 바닥면 회전은 평가하고, 높이 축 변경은 `눕힘 허용` 제품에만 적용한다.
3. 부피, 바닥면, 중량과 안정성 위험도를 이용해 탐색 순서를 정한다.
4. 컨테이너 바닥과 이미 배치한 화물의 끝점에서 Extreme Point 후보를 만든다.
5. 후보마다 경계, 충돌, 중량, 상부 적재 금지, 바닥 지지, 중심 지지와 측면 지지를 검사한다.
6. 유효 후보를 DBLF 원칙인 낮은 위치 → 안쪽 → 좌측 순서와 공간 점수로 비교한다.
7. 한 컨테이너에 들어가지 않은 화물은 같은 규격의 다음 컨테이너에서 다시 계산한다.
8. 확정된 배치로 적재 순서와 각재·부목·에어백·래싱 밴드 권고를 만든다.

같은 입력과 설정에는 같은 결과를 내도록 결정론적으로 동작한다. 현재 알고리즘 식별자는 `extreme-dblf-width-2026.08`이다. 저장 프로젝트에는 이 식별자와 계산 결과 요약을 함께 기록하며, 알고리즘 버전이 바뀌면 결과도 달라질 수 있다.

무게중심은 배치된 화물의 기하학적 중심에 개당 중량을 곱한 정적 모멘트로 계산한다. 컨테이너 중심 대비 전후·좌우 편차가 모두 5% 이하면 `안정`, 최대 편차가 10% 이하면 `주의`, 그보다 크면 `위험`으로 표시한다. 이 판정은 축하중, 바닥 집중하중, 마찰, 가감속과 운송 중 동하중을 포함하지 않는다.

## 3. 공통 하드 조건

다음 조건을 통과하지 못한 후보는 점수가 좋아도 배치하지 않는다.

- 컨테이너 내부 경계를 넘지 않음
- 다른 화물과 3차원 영역이 겹치지 않음
- 컨테이너 최대 적재 중량을 넘지 않음
- 상부 적재 금지 화물 위에 다른 화물을 놓지 않음
- 상부 화물의 중심이 실제 지지 영역 안에 있음
- 원통 위 박스는 배치하지 않음
- 원통 위 원통은 중심 오차 15mm 이하, 지름 차이 합계 30mm 이하
- 입력 수량은 적재 수량과 미배치 수량의 합과 일치

## 4. 최적화 모드

### 안전 적재

- 바닥 배치를 먼저 채운다.
- 상부 적재는 바닥면 지지율 100%를 요구한다.
- 취약 화물과 불안정한 세장 화물 위 배치를 보수적으로 제외한다.
- 누적 높이가 바닥 최소 치수의 1.5배를 넘는 화물은 최소 2방향 측면 지지를 요구한다.

### 최대 적재

- 안전 적재안에 추가하는 방법과 전체 고밀도 배치안을 함께 비교한다.
- 상부 적재 최소 지지율은 70%이며 중심 지지와 취약 화물 조건은 유지한다.
- 적재 수량을 우선하고, 수량이 같으면 사용 깊이가 짧은 안을 선택한다.

### 자동 추천

취약 화물, 원통, 세장 화물, 예상 공간 점유율과 회전 가능 비율을 바탕으로 안전 적재 또는 최대 적재를 선택한다. 자동 추천은 별도의 제3 알고리즘이 아니라 현재 두 전략 중 하나를 고르는 규칙이다.

### 폭 우선 적재

- 컨테이너 긴 방향의 사용 길이보다 짧은 방향의 잔여 폭 최소화를 우선한다.
- 남은 화물 폭으로 채우고도 남는 폭, 화물 사이 내부 틈, 현재 빈 폭, 사용 높이, 긴 방향 사용 길이 순으로 후보를 비교한다.
- 상부 적재는 안전 적재와 동일하게 지지율 100%, 중심 지지와 세장 화물 조건을 적용한다.
- 폭을 먼저 채우는 휴리스틱이므로 최대 적재 수량이나 최소 사용 길이를 보장하지 않는다.

## 5. 측면 지지와 고정재

- 전·후·좌·우 각 방향에서 화물 면의 폭 또는 높이가 50% 이상 맞닿을 때 해당 방향 지지로 인정한다.
- 높은 세장 화물에서 1~2방향 지지가 부족하면 부족 방향에 래싱 밴드를 필수 권고한다.
- 3방향 이상이 부족하면 래싱만으로 보완하지 않고 배치 후보에서 제외한다.
- 문쪽 노출 화물에는 가로 각재와 부목을 우선 권고한다.
- 벽면 간극 100mm 이상 또는 화물 사이 간극 120~600mm에는 충돌이 없을 때 에어백을 권고한다.
- 이 권고는 실제 래싱 강도, 밴드 수량, 에어백 압력이나 바닥 고정 허용 여부를 계산한 설계가 아니다.

## 6. 성공·부분 적재·실패 판정

| 상태 | 기준 |
|---|---|
| 적재 완료 | 입력 화물이 모두 배치됨 |
| 부분 적재 | 일부는 배치됐지만 미배치 화물이 남음 |
| 적재 불가 | 배치된 화물이 없고 미배치 화물이 존재함 |

계산 진행률 100%는 계산 작업이 끝났다는 뜻이며 적재 성공률과 다르다. 미배치 화물이 있으면 Excel 내보내기를 허용하지 않는다.

## 7. 현재 계산하지 않는 항목

- 제품별 압축강도, 최대 상부 하중과 누적 하중
- 전체 화물 무게중심, 바닥 집중하중과 차량 축하중
- 마찰계수, 급제동·충격·진동과 운송 중 이동량
- 래싱 용량, 밴드 각도·수량, 에어백 압력과 파열 한계
- 팔레트 강도, 포장 변형과 원통의 실제 곡면 접촉
- 위험물 격리, 냉동 컨테이너 공기 순환과 법규 적합성
- 컨테이너 실측 오차, 돌출물, 문 개구 치수와 작업 장비 공간
- 모든 배치 조합을 탐색하는 전역 최적화

## 8. 알고리즘 추가·변경 정책

새 알고리즘이나 점수 규칙은 다음 조건을 만족해야 배포한다.

1. 공통 하드 조건을 우회하지 않는다.
2. 기존 전략과 같은 입력으로 비교 가능한 결과를 만든다.
3. 적재 수량뿐 아니라 경계·충돌·중량·지지 불변식 테스트를 통과한다.
4. 실패와 미배치 이유를 사용자에게 숨기지 않는다.
5. 결과가 크게 달라지는 변경은 이 문서의 버전과 변경 이력에 기록한다.
6. 실험 전략은 기본 전략과 구분하고 사용자가 선택 여부를 알 수 있게 한다.

## 9. 변경 이력

| 문서 버전 | 날짜 | 주요 내용 |
|---|---|---|
| 1.0 | 2026-08-11 | Extreme Point·DBLF, 안전/최대/자동 전략, 측면 지지·래싱과 현재 한계 정리 |
| 1.1 | 2026-08-11 | 저장 스키마 v2, 알고리즘 식별자와 결과 요약 보존 정책 추가 |
| 1.3 | 2026-08-11 | 저장 스키마 v3, 무게중심 판정과 현장 결과 비교 정책 추가 |
| 1.2 | 2026-08-11 | 짧은 방향 잔여 폭을 우선 최소화하는 폭 우선 적재 전략 추가 |
