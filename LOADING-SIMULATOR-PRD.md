# LoadWise v3 컨테이너 로딩 시뮬레이터 PRD

- 문서 버전: 3.2
- 갱신일: 2026-08-11
- 제품 단계: Vercel 공개 베타
- v3 서비스: https://loadwise-v3.vercel.app/
- v3 저장소: https://github.com/cowpigpapa/loading-simulator_v3
- 알고리즘 정책: [ALGORITHM-POLICY.md](ALGORITHM-POLICY.md)
- 운영 중인 v2: https://cowpigpapa.github.io/loading-simulator_v2/

## 1. 제품 정의

LoadWise는 제품의 규격·중량·수량·형상·적층 제약을 입력하면 선택한 컨테이너에 대한 배치, 적재 순서, 컨테이너 수량과 화물 고정재 위치를 계산하고 3D로 보여주며, 입력 프로젝트를 저장해 다시 사용할 수 있는 웹 기반 적재 계획 도구다.

### 문제 정의(SCR)

- **Situation:** 물류 담당자는 서로 다른 제품을 제한된 컨테이너 공간에 안전하게 적재해야 한다.
- **Complication:** 수작업 계획은 회전 조합, 상부 지지, 빈 공간, 작업 순서와 고정재 위치를 동시에 판단하기 어렵고 작업자 경험에 따라 결과가 달라진다.
- **Resolution:** 브라우저에서 제품 데이터와 제약을 검사하고, 휴리스틱 기반 배치 후보를 평가해 컨테이너별 3D 계획과 Excel 작업표를 생성한다.

## 2. 목표

1. 제품 입력부터 3D 적재 계획 생성까지 한 화면에서 완료한다.
2. 안전 적재, 최대 적재와 폭 우선 적재의 목적을 명확히 구분한다.
3. 빈 공간이 있어도 위험하게 상부 적재되는 결과를 방지한다.
4. 단일 컨테이너가 부족하면 같은 규격의 복수 컨테이너로 자동 분할한다.
5. 문에서 가장 안쪽 화물부터 작업할 수 있는 적재 순서를 제공한다.
6. 화물 이동 방지를 위한 문쪽 각재·부목과 벽면·화물 간 에어백 위치를 추천한다.
7. 입력 프로젝트를 간단히 저장하고 목록에서 다시 불러온다.

## 3. 비목표 및 안전 고지

- 전역 최적해를 수학적으로 보장하지 않는다.
- 실제 제품의 압축강도, 포장재 마찰계수, 동적 충격과 진동을 계산하지 않는다.
- 차량 축하중, 선박 복원성, 위험물 격리, 냉동 공기 순환을 인증하지 않는다.
- 현재 결과는 작업 검토용이며 실제 상차 전 현장 책임자와 고정재 제조사 지침으로 검증해야 한다.

## 4. 핵심 사용자

| 사용자 | 핵심 업무 | 기대 가치 |
|---|---|---|
| 수출·물류 담당자 | 컨테이너 규격 및 대수 산정 | 견적과 출하 계획 시간 단축 |
| 창고·상차 작업자 | 적재 위치와 순서 확인 | 작업 재판단과 재배치 감소 |
| 물류 관리자 | 계획 검토와 표준화 | 안전 기준 및 결과 형식 통일 |
| 영업·견적 담당자 | 선적 가능 수량 예측 | 초기 물류비 추정 정확도 향상 |

## 5. 사용자 흐름

1. 제품을 직접 등록하거나 Excel/CSV 파일을 업로드한다.
2. 등록 제품의 수량, 눕힘 허용, 상부 적재 금지를 목록에서 수정한다.
3. 20ft, 40ft, 40ft High Cube, 45ft High Cube 중 규격을 선택한다.
4. `안전 적재`, `최대 적재`, `폭 우선` 또는 `자동 추천`을 선택한다.
5. 어느 위치의 `시뮬레이션 실행` 버튼이든 눌러 같은 계산을 실행한다.
6. 진행률과 완료 시간을 확인한다.
7. 컨테이너별 3D, 상면, 단계별 적재 순서와 고정재를 확인한다.
8. 컨테이너가 여러 대면 `‹ 현재 / 전체 ›`로 이동한다.
9. 전체 계획을 Excel로 내보낸다.
10. 첫 저장에서 이름을 정하고 이후에는 같은 프로젝트를 바로 갱신한다.
11. 저장 프로젝트를 불러오면 저장된 조건으로 시뮬레이션을 자동 실행한다.
12. 컨테이너별 무게중심과 전후·좌우 중량 배분을 확인한다.
13. 3D·상면·문 기준·좌측면·우측면 시점을 확인한다.
14. 작업지시서를 새 창에서 검토한 뒤 인쇄하거나 PDF로 저장한다.
15. 출력된 작업지시서에 실제 적재 수량·컨테이너 수·추가 고정재·특이사항을 기록한다.

### FR-00 프로젝트 저장·계정 보안

- 직접 등록과 Excel/CSV 업로드 결과는 같은 프로젝트 데이터 모델을 사용한다.
- 첫 저장 때 파일명 또는 첫 제품명을 기본 저장 이름으로 제안한다.
- 이후 `저장`은 확인을 거쳐 현재 프로젝트를 갱신하고, `신규 저장`은 현재 작업을 새 프로젝트로 저장한다.
- `저장 목록`에서 프로젝트 불러오기·삭제를 제공한다.
- 저장하지 않은 변경사항을 버리는 동작은 사용자 확인을 거친다.
- `새 프로젝트`는 제품 목록, 컨테이너 설정과 기존 시뮬레이션 결과를 모두 초기화한다.
- 비로그인 상태에서도 브라우저 `localStorage`에 저장·목록·불러오기를 제공하고 로컬 저장임을 화면에 명시한다.
- 이메일 매직 링크로 로그인하며 상단 계정 버튼은 로그인 상태에 따라 로그인·로그아웃으로 전환한다.
- Google OAuth 또는 8자리 이메일 OTP 로그인 상태의 저장·목록은 Supabase의 사용자별 클라우드 저장소를 사용한다.
- Microsoft OAuth 운영 계정은 `loadwise.app@outlook.com`으로 생성했으나, 개인 계정의 Entra 앱 신규 등록 제한과 Azure 디렉터리 가입 시 전화번호·결제카드 본인 확인 부담으로 2026-08-13 설정을 보류했다. Entra 및 Supabase 설정 완료 전까지 관련 로그인 버튼은 노출하지 않는다.
- 저장 프로젝트를 불러오면 제품 데이터와 컨테이너 설정을 적용한 후 기존 시뮬레이션 실행 경로를 자동 호출한다.
- 저장 데이터에 스키마 버전, 알고리즘 버전과 마지막 계산 결과 요약을 기록한다.
- 로그인 상태에서는 계정 이메일과 클라우드 저장 위치를 화면에 표시한다.
- 클라우드 데이터는 `user_id`를 소유자로 기록하고 PostgreSQL RLS에서 `auth.uid() = user_id`를 강제한다.
- 브라우저에는 publishable key만 제공하며 service role key는 포함하지 않는다.

### FR-00A 관리자와 접속 통계

- `juan.hjlee@gmail.com`을 최초 관리자로 등록한다.
- 관리자 권한은 Supabase에서 로그인 토큰의 이메일과 관리자 이메일 목록을 비교해 판정한다.
- 로그인 사용자의 이메일, 현재 저장 프로젝트 수, 완료된 시뮬레이션 누계, 최초·최근 접속과 접속 횟수를 관리자에게만 제공한다.
- 관리자는 이메일을 기준으로 다른 사용자의 관리자 권한을 추가·해제할 수 있다.
- 관리자는 자신의 권한을 화면에서 해제할 수 없다.
- 비로그인 방문자는 개인 식별 정보를 저장하지 않고 기존 일일·누적 방문 통계에만 포함한다.
- 관리자 목록과 접속 통계는 RLS 및 관리자 검증 함수를 통과한 사용자만 조회할 수 있다.

## 6. 기능 요구사항

### FR-01 제품 입력·관리

- 필드: 제품명, 제품군, 제품 형상, 수량, 개당 중량, 길이, 너비, 높이, 눕힘 허용, 상부 적재 금지.
- 형상은 박스형과 원통형을 지원한다.
- 기본값은 `눕힘 허용 안 함`이다.
- 등록된 제품 수량은 숫자 직접 입력과 위·아래 화살표로 변경한다.
- 등록 후 눕힘 허용과 상부 적재 금지를 수정할 수 있다.
- 데이터가 바뀌면 모든 실행 버튼에 재계산 필요 상태를 표시한다.

### FR-02 Excel/CSV 입력

- `.xlsx`, `.xls`, `.csv`를 지원한다.
- 한글·영문 헤더를 표준 필드로 매핑한다.
- 필수 규격이나 중량이 없거나 0 이하이면 해당 행을 제외하고 오류를 알린다.
- 샘플 CSV 템플릿을 제공한다.

### FR-03 컨테이너

| 규격 | 내부 길이 | 내부 폭 | 내부 높이 | 최대 적재 중량 |
|---|---:|---:|---:|---:|
| 20ft Dry | 5,898mm | 2,352mm | 2,393mm | 28,200kg |
| 40ft Dry | 12,032mm | 2,352mm | 2,393mm | 26,700kg |
| 40ft High Cube | 12,032mm | 2,352mm | 2,698mm | 26,500kg |
| 45ft High Cube | 13,556mm | 2,352mm | 2,698mm | 27,600kg |

- 화물이 한 대에 들어가지 않으면 같은 규격의 다음 컨테이너에 반복 배치한다.
- 최대 50대까지 결과를 생성한다.
- 제품 자체가 선택 컨테이너에 들어가지 않으면 별도 검토 대상으로 표시한다.

### FR-04 공통 배치 제약

- 컨테이너 경계, 제품 간 충돌, 최대 적재 중량을 하드 제약으로 적용한다.
- 바닥면 회전은 항상 허용하며 `눕힘 허용`은 높이 축을 바꾸는 회전을 의미한다.
- 상부 적재 금지 제품 위에는 다른 제품을 놓지 않는다.
- 후보 위치는 배치 후 생성되는 Extreme Point를 사용한다.
- 기본 탐색 순서는 낮은 Z → 안쪽 X → 좌측 Y의 DBLF 원칙을 따른다.
- 동일 조건에서는 가로 공백, 바닥 지지와 사용 깊이를 평가한다.
- 큰 제품과 위험도가 높은 제품은 가능한 한 하부와 안쪽에 우선 배치한다.

### FR-05 안전 적재 모드

- 바닥 배치를 먼저 최대한 채운 뒤 상부 후보를 검토한다.
- 상부 배치의 지지율은 100%를 하드 조건으로 적용한다.
- 화물 중심점이 실제 지지 접촉 영역 안에 있어야 한다.
- 불안정한 세장비의 상부 배치를 제외한다.
- 옆의 빈 바닥 공간보다 상부 적재를 먼저 선택하지 않는다.

### FR-06 최대 적재 모드

- 안전 적재 결과를 시작점으로 추가 적재하는 안과 전체를 새로 고밀도 배치하는 안을 함께 평가한다.
- 상부 적재 금지가 아닌 제품은 적극적으로 2단 이상 후보를 평가한다.
- 최소 지지율 70%, 중심 지지, 하부 취약 화물 금지는 하드 조건으로 유지한다.
- 적재 수량을 1순위로 비교하고 같은 수량이면 사용 깊이가 짧은 결과를 선택한다.
- 공간이 남았는데 한 줄만 길게 배치하는 결과를 줄이도록 내부 공백과 가로 잔여 폭을 평가한다.

### FR-06A 폭 우선 적재 모드

- 긴 방향의 사용 길이가 늘어나더라도 짧은 방향의 잔여 폭을 우선 최소화한다.
- 남은 제품 폭으로 채운 뒤의 예상 잔여 폭, 내부 틈, 현재 잔여 폭 순으로 배치 후보를 평가한다.
- 상부 지지율 100%, 중심 지지, 취약 화물과 세장 화물 조건은 안전 적재 모드와 동일하게 유지한다.
- 폭 우선은 휴리스틱 선택 기준이며 최대 적재 수량이나 최소 컨테이너 수를 보장하지 않는다.

### FR-07 원통형 적재

- 원통은 실제 원통 메시로 표시하되 충돌 계산에는 외접 영역을 사용한다.
- 박스 위 원통은 평면 지지율 조건을 만족하면 허용한다.
- 원통 위 박스는 곡면 지지로 판단해 금지한다.
- 원통 위 원통은 중심 오차 15mm 이하, 지름 차이 합계 30mm 이하일 때만 허용한다.
- 누적 높이가 바닥면 최소 치수의 1.5배를 넘는 화물은 전·후·좌·우 중 최소 2방향의 측면 지지를 확보해야 한다.
- 각 방향의 측면 지지는 해당 면의 폭 또는 높이가 50% 이상 맞닿을 때 인정한다.
- 화물 자체 높이 비율이 1.5를 넘거나 누적 적재 높이가 짧은 바닥변의 2배를 넘는 화물에서 지지가 부족한 방향이 1~2개이면 해당 방향 래싱 밴드를 고정재로 추천한다.
- 지지가 부족한 방향이 3개 이상이면 래싱만으로 보완하지 않는다. 고세장 화물은 배치 후보에서 제외하고, 그 밖의 2단 이상 화물은 결과에서 배치 재검토 대상으로 표시한다.
- 원통 상부 적재는 바닥 지지율, 중심 지지 및 측면 지지 조건을 모두 충족하면 허용한다.

### FR-08 적재 순서

- 실제 작업 순서는 컨테이너 안쪽에서 문쪽 방향으로 생성한다.
- 같은 깊이에서는 아래 화물을 먼저 표시한다.
- 이전, 다음, 자동재생, 슬라이더로 0단계부터 완료 단계까지 볼 수 있다.
- 하부 화물이 배치되기 전에 상부 화물이 공중에 나타나지 않아야 한다.

### FR-09 3D 시각화

- Three.js WebGL로 컨테이너 육면체, 박스, 원통, 부목과 에어백을 표현한다.
- 드래그 회전, 휠 확대·축소, 3D, 상면, 보기 초기화를 제공한다.
- X는 문에서 안쪽, Y는 좌측에서 우측, Z는 바닥에서 위를 뜻한다.
- 부목과 에어백은 각각 ON/OFF 토글을 제공하고 선택 상태를 시각적으로 표시한다.

### FR-10 화물 고정재 추천

- 문쪽에 노출된 화물 폭 전체를 받치는 가로 각재를 표시한다.
- 노출 폭에 따라 부목 3~6개를 좌·중·우에 균등 분산하고 바닥 못 고정 형태로 표현한다.
- 좌측·우측·안쪽 벽과 화물 사이의 연속 빈 공간이 100mm 이상이면 바닥부터 에어백을 추천한다.
- 벽면 간극이 600mm를 넘으면 각 백이 600mm 이하가 되도록 여러 개로 분할한다.
- 화물 사이 에어백은 간극 120~600mm이고 충돌 없는 경우 추천한다.
- 에어백은 공중에 단독 배치하지 않고 바닥부터 해당 높이까지 이어지게 표현한다.
- 문쪽 잔여 공간은 에어백보다 가로 각재와 부목을 우선한다.

### FR-11 결과 및 Excel 내보내기

- 공간 활용률, 중량 활용률, 적재 수량, 총 적재 중량을 표시한다.
- Excel은 `요약`, `적재계획`, `고정재`, `제품목록` 정보를 포함한다.
- 적재계획에는 컨테이너 번호, 적재 순서, 제품 정보, X·Y·Z, 회전 후 규격과 중량을 포함한다.
- 고정재 시트에는 종류, 관련 제품, 추천 위치와 치수를 포함한다.

### FR-12 실행 상태 및 방문자

- 계산 중 진행률, 계산 대상 컨테이너 번호와 완료 시간을 표시한다.
- 긴 계산 중 브라우저가 응답 없음처럼 보이지 않도록 주기적으로 UI 스레드에 제어를 반환한다.
- 공개 사이트에 Supabase 원자적 카운터를 이용해 한국 시간 기준 오늘 방문자와 누적 방문자를 표시한다.

## 7. 시스템·알고리즘 구조

| 모듈 | 책임 |
|---|---|
| 입력 UI | 직접 입력, 파일 업로드, 목록 수정과 검증 |
| 회전 생성기 | 제품별 허용 방향 조합 생성 |
| Extreme Point 엔진 | 배치 후 신규 후보 위치 생성 |
| 제약 검사기 | 경계, 충돌, 중량, 취약 제품, 지지율과 중심 지지 검사 |
| 점수 평가기 | 가로 공백, 내부 공백, 깊이, 높이와 지지율 비교 |
| 다중 컨테이너 엔진 | 미배치 제품을 다음 컨테이너에 반복 배치 |
| 고정재 엔진 | 문쪽 각재·부목과 벽면·화물 간 에어백 추천 |
| 3D 뷰어 | 단계별 배치와 고정재 렌더링 |
| Excel 내보내기 | 계획·제품·고정재 데이터를 작업 문서로 변환 |
| 프로젝트 저장 | 비로그인 로컬 저장과 로그인 사용자별 Supabase 저장·불러오기 |
| 인증·권한 | 이메일 매직 링크 로그인과 PostgreSQL RLS 사용자 격리 |

현재 서비스는 Vercel에 배포된 정적 SPA이며 적재 계산과 업로드 파일 처리는 브라우저에서 수행한다. 비로그인 프로젝트는 브라우저에, 로그인 프로젝트는 Supabase Postgres에 저장한다. 업로드 원본 파일 자체는 서버로 전송하지 않고 정규화된 프로젝트 데이터만 사용자가 저장할 때 저장한다.

## 8. 수용 기준

| ID | 검증 조건 | 기대 결과 |
|---|---|---|
| AC-01 | 제품이 컨테이너 경계를 넘는 입력 | 해당 방향 후보 제외 |
| AC-02 | 두 제품의 3D 영역이 겹침 | 배치 후보 제외 |
| AC-03 | 안전 모드 상부 지지율 99% | 배치 후보 제외 |
| AC-04 | 최대 모드 상부 지지율 69% | 배치 후보 제외 |
| AC-05 | 상부 적재 금지 제품 위 후보 | 배치 후보 제외 |
| AC-06 | 원통 위 박스 | 배치 후보 제외 |
| AC-07 | 중심이 어긋난 원통 위 원통 | 배치 후보 제외 |
| AC-08 | 벽 간극 90mm | 에어백 미추천 |
| AC-09 | 벽 간극 110mm | 에어백 추천 |
| AC-10 | 벽 간극 1,000mm | 600mm 이하 백 2개 이상으로 분할 |
| AC-11 | 여러 컨테이너 필요 | `‹ 1 / N ›`으로 모두 탐색 가능 |
| AC-12 | 적재 재생 | 하부 → 상부, 안쪽 → 문쪽 순서 유지 |
| AC-13 | 옵션 또는 수량 변경 | 실행 버튼 재계산 상태 활성화 |
| AC-14 | Excel 내보내기 | 모든 컨테이너와 고정재가 파일에 포함 |
| AC-15 | 전·후·좌·우 중 2방향 이상 지지 | 배치 허용, 부족한 1~2방향 래싱 추천 |
| AC-16 | 전·후·좌·우 중 3방향 이상 지지 부족 | 배치 후보 제외 |
| AC-17 | 후면·좌측 지지, 전면·우측 미지지 | `전·우` 방향 래싱 밴드 추천 |
| AC-18 | 비로그인 상태에서 프로젝트 저장 | 현재 브라우저의 저장 목록에 표시 |
| AC-19 | 로그인 상태에서 프로젝트 저장 | 해당 사용자 계정의 클라우드 목록에만 표시 |
| AC-20 | 저장 목록에서 프로젝트 불러오기 | 별도 재계산 클릭 없이 3D와 결과 자동 갱신 |
| AC-21 | 구버전 저장 데이터 불러오기 | 현재 스키마로 안전하게 정규화 |
| AC-22 | 프로젝트 저장 | 알고리즘 버전과 마지막 계산 요약 포함 |

## 9. KPI

| KPI | 측정 방법 | 초기 목표 |
|---|---|---:|
| 계산 성공률 | 오류 없이 결과가 생성된 실행 / 전체 실행 | 98% 이상 |
| 기하 오류율 | 경계 초과 또는 제품 중첩 결과 / 전체 배치 | 0% |
| 안전 모드 지지 위반 | 바닥 지지율 또는 최소 2방향 측면 지지 조건 위반 상부 제품 수 | 0건 |
| 계획 생성 시간 | 200개 화물 기준 중앙 완료 시간 | 5초 이내 |
| 입력 완료 시간 | 업로드부터 결과 확인까지 중앙값 | 5분 이내 |
| 결과 이해도 | 사용자 테스트에서 순서·좌표를 정확히 설명한 비율 | 90% 이상 |
| 재방문율 | 일별 방문자 중 재방문 사용자 비율 | 출시 후 기준선 설정 |

## 10. 현재 구현 상태

| 범위 | 상태 |
|---|---|
| 직접 입력, Excel/CSV 업로드 | 구현 |
| 제품 수량·적층 옵션 수정 | 구현 |
| 표준 컨테이너 4종 및 다중 컨테이너 | 구현 |
| 안전 적재·최대 적재·폭 우선 적재 | 구현 |
| Extreme Point, DBLF, 지지율·중심 지지 | 구현 |
| 박스·원통 3D와 단계별 재생 | 구현 |
| 각재·부목·에어백·래싱 밴드 추천 및 토글 | 구현 |
| 진행률·완료 시간 | 구현 |
| Excel 계획 내보내기 | 구현 |
| 일일·누적 방문자 | 구현 |
| 제품별 허용 상부 하중 | 미구현 |
| 전체 화물 무게중심·축하중 | 미구현 |
| 비로그인 브라우저 저장·불러오기 | 구현 |
| 이메일 로그인·로그아웃 | 구현 |
| Supabase 사용자별 저장과 RLS | 구현 |
| 저장 프로젝트 불러오기 후 자동 계산 | 구현 |
| 저장 스키마 v3·알고리즘 버전·결과 요약 | 구현 |
| Playwright UI 회귀 테스트와 CI | 구현 |

## 11. 로드맵

### P0 — 계산 신뢰성

- 제품별 `최대 상부 하중`, 허용 적층 단수 필드 추가
- 상부 누적 하중을 하부 제품별로 전달해 파손 위험 차단
- 실제 접촉 영역 합집합과 지지 다각형 기반 중심 안정성 강화
- 대표 시나리오 회귀 테스트 확대

### P1 — 적재 품질

- Beam Search 또는 다중 시작 휴리스틱으로 제품 순서 후보 확대
- 공간을 여러 구역으로 분할하는 wall/layer 패턴과 Extreme Point 결과 비교
- 전체 무게중심과 전후·좌우 중량 편차 표시
- 결과별 안전 점수와 선택 이유 제공

### P2 — 운영 기능

- 프로젝트 변경 이력과 계획 버전 비교
- 사용자 정의 컨테이너·팔레트·고정재 규격
- 작업지시서 양식과 다중 컨테이너 출력 고도화
- WMS/ERP/TMS API 연동

## 12. 주요 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 휴리스틱의 국소 최적해 | 더 좋은 배치를 놓칠 수 있음 | 여러 전략 결과를 비교해 최선안 선택 |
| 제품 강도 데이터 부재 | 하부 제품 파손 가능 | 최대 상부 하중 입력과 누적 하중 검사 |
| 외접 영역 기반 원통 충돌 | 공간 활용률 보수적 계산 | 원통 전용 충돌·접촉 모델 고도화 |
| 고정재 추천 단순화 | 실제 압력·용량과 다를 수 있음 | 제조사 사양 필드와 현장 승인 절차 추가 |
| 브라우저 단일 스레드 계산 | 대량 입력 시 지연 | Web Worker로 계산 엔진 분리 |
| 외부 CDN/방문자 API | 네트워크 장애 시 일부 기능 제한 | 라이브러리 로컬 번들 및 장애 시 대체 표시 |
| 비로그인 로컬 저장 | 브라우저 데이터 삭제·공용 프로필 사용 시 손실 또는 노출 | 화면 안내, 중요 프로젝트 로그인 저장 권고 |
| 이메일 링크 전달 | 메일 지연 또는 링크를 연 기기에서 세션 전환 | 재전송 안내와 허용된 리디렉션 URL 관리 |

## 13. 출시 게이트

1. 대표 혼합 화물 시나리오에서 경계 초과와 제품 중첩이 0건이어야 한다.
2. 안전 모드에서 상부 화물 지지율 100%와 중심 지지 조건을 모두 만족해야 한다.
3. 적재 순서상 상부 화물이 하부보다 먼저 표시되지 않아야 한다.
4. Chrome과 Edge의 데스크톱·좁은 화면에서 입력, 실행, 3D와 내보내기가 동작해야 한다.
5. Excel 결과로 모든 컨테이너의 배치와 고정재 위치를 식별할 수 있어야 한다.
6. 200개 화물 계산 중 UI 진행 상태가 계속 갱신되어야 한다.
7. 화면과 내보내기 문서에 현장 안전 검토 필요성을 고지해야 한다.
8. 알고리즘 변경 시 `ALGORITHM-POLICY.md`의 판정 기준, 한계와 변경 이력을 함께 갱신해야 한다.

---

본 PRD는 2026-08-11 현재 Vercel 공개 베타와 코드 구현을 기준으로 한다.
