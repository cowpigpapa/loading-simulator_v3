# LoadWise 알고리즘 개선·학습 운영안

## 원칙

컨테이너 적재에는 하나의 보편적 정답 휴리스틱이 없다. LoadWise는 알고리즘 하나를 학습시키기보다 여러 후보 알고리즘이 동일한 입력에서 경쟁하고, 독립 검증기를 통과한 결과만 비교하는 구조로 운영한다.

안전 규칙은 학습 대상이 아니다. 경계, 충돌, 중량, 회전 허용, 지지, 상부적재 금지, 입력 수량 보존은 항상 하드 게이트로 유지한다. 학습기는 검증을 통과한 해 중 무엇을 먼저 실행하고 어떤 파라미터를 사용할지만 선택한다.

## 데이터 루프

1. 입력을 익명 특징으로 변환한다: 화물 수, SKU 수, 체적·중량 분포, 규격 다양도, 회전 가능 비율, 원통·취약·세장 화물 비율, 컨테이너 규격.
2. 모든 후보 전략의 결과를 기록한다: 엔진 버전, seed, 반복 수, 적재·미배치 수량, 컨테이너 수, 활용률, CoG 편차, 안전 경고, 실행시간, validator 결과.
3. 현장 사용자가 계획을 채택·수정·거부했는지와 수정 사유를 선택적으로 기록한다. 제품명과 고객명은 학습 데이터에서 제외한다.
4. 검증 실패 결과는 즉시 폐기한다. 통과 결과만 사전식 목적함수로 순위를 정한다.
5. 새 선택기는 기존 자동 추천과 shadow 비교한 뒤, benchmark와 현장 지표가 모두 개선될 때만 champion으로 승격한다.

## 학습 단계

### 1단계 — 학습 없는 포트폴리오

현재 전략과 향후 EP multi-start, beam, repair, block을 동일 benchmark에서 실행한다. 입력 유형별 승률표를 만들고 규칙 기반으로 실행 순서를 정한다. 이 단계가 충분히 강하면 ML은 필요 없다.

### 2단계 — 지도학습 전략 선택기

각 입력의 특징을 X, validator를 통과한 최우수 전략을 y로 삼는다. 처음에는 작은 decision tree 또는 gradient boosting으로 ‘어떤 전략을 먼저 실행할지’만 예측한다. 예측이 틀려도 시간 예산 안에서 다른 전략을 계속 비교하므로 결과 안전성은 변하지 않는다.

### 3단계 — Contextual bandit

현장 채택 데이터가 충분해지면 입력 특징을 context, 선택한 전략을 action, 검증된 목적 점수와 현장 채택을 reward로 사용한다. 신규 전략에는 제한된 탐색 트래픽을 주되 기본 결과에는 영향을 주지 않는 shadow 모드부터 시작한다.

강화학습이나 생성형 AI가 좌표를 직접 만드는 방식은 보류한다. 현장 데이터가 적고, 잘못된 좌표의 비용이 크며, 현재 휴리스틱+validator 구조보다 설명과 회귀 검증이 어렵다.

## 승격 기준

새 엔진은 다음 조건을 모두 만족해야 한다.

- 독립 validator 통과율 100%
- 기존 안전 회귀 테스트 100% 통과
- 미배치 수와 컨테이너 수가 baseline보다 악화되지 않음
- 대표 benchmark의 활용률 또는 CoG·작업성 지표 개선
- p95 실행시간이 제품 시간 예산 이내
- 동일 입력·버전·seed의 결정론적 재현
- 알고리즘 정책과 한계 문서 업데이트

## 최소 저장 스키마

```json
{
  "inputHash": "...",
  "features": {"items": 36, "sku": 4, "rotationRate": 0.75, "fragileRate": 0.1},
  "engineVersion": "...",
  "strategy": "sequence",
  "seed": 1234,
  "metrics": {"unallocated": 0, "containers": 1, "volumeRate": 82.1, "cogRisk": 4.2, "elapsedMs": 420},
  "valid": true,
  "fieldOutcome": "accepted"
}
```

사용자 동의를 받은 현장 결과만 서버에 저장한다. 원본 Excel, 제품명, 고객명, 이메일은 학습 데이터에 포함하지 않는다.

## 현재 구현된 기반

- `solution-validator.js`: 생성 엔진과 분리된 경계·충돌·중량·수량·지지·스택 합성 CoG 검사
- `benchmark.mjs`: portfolio와 단일 시작을 동일 사례에서 비교하고 JSON 결과 보존
- CI gate: validator 실패, 미배치 증가, 컨테이너 수 증가 또는 무게중심 전략의 CoG 악화 시 실패
- 결정론적 portfolio: 체적·최장변·바닥면 순서를 비교하되 자동 추천은 응답시간을 위해 1차 탐색을 사용

아직 현장 데이터 자동 수집과 ML 모델 학습은 구현하지 않았다. 사용자 동의·익명화·최소 표본 기준이 확정되기 전까지는 benchmark 결과만 축적한다.
